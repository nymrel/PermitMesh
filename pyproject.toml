[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "permitmesh"
version = "0.2.0"
description = "A portable policy-decision profile for AI agents changing software."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Jalen Studio" }]
keywords = ["ai-agents", "authorization", "nostr", "buzz", "policy"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.13",
  "Topic :: Security",
]
dependencies = []

[project.optional-dependencies]
test = [
  "bandit>=1.8,<2",
  "build>=1.2,<2",
  "jsonschema[format]>=4.23,<5",
  "mypy>=1.19,<2",
  "ruff>=0.14,<1",
]

[project.scripts]
permitmesh = "permitmesh.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff.lint]
# Ruff 0.16 expanded its implicit default from 59 to 413 rules. Keep the
# repository's existing pre-0.16 lint contract explicit so tool upgrades do not
# silently redefine CI policy; new rules can be adopted intentionally later.
select = ["E4", "E7", "E9", "F"]
