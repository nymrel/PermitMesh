{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/nymrel/PermitMesh/v0.2.0/schema/permitmesh-conformance-receipt.schema.json",
  "title": "PermitMesh Conformance Receipt 0.2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "receipt_version",
    "implementation",
    "suite",
    "generated_at",
    "enforcement_boundary",
    "summary",
    "cases"
  ],
  "properties": {
    "receipt_version": {"const": "0.2"},
    "implementation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "version"],
      "properties": {
        "name": {"type": "string", "minLength": 1, "maxLength": 128},
        "version": {"type": "string", "minLength": 1, "maxLength": 128}
      }
    },
    "suite": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "version", "digest"],
      "properties": {
        "name": {"type": "string", "minLength": 1, "maxLength": 128},
        "version": {"const": "0.2"},
        "digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
      }
    },
    "generated_at": {"type": "string", "format": "date-time"},
    "enforcement_boundary": {"type": "string", "minLength": 1, "maxLength": 512},
    "summary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["total", "passed", "failed"],
      "properties": {
        "total": {"type": "integer", "minimum": 1, "maximum": 256},
        "passed": {"type": "integer", "minimum": 0, "maximum": 256},
        "failed": {"type": "integer", "minimum": 0, "maximum": 256}
      }
    },
    "cases": {
      "type": "array",
      "minItems": 1,
      "maxItems": 256,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "expected_outcome",
          "observed_outcome",
          "passed"
        ],
        "properties": {
          "id": {"type": "string", "minLength": 1, "maxLength": 128},
          "expected_outcome": {"enum": ["allow", "deny", "malformed"]},
          "observed_outcome": {"enum": ["allow", "deny", "malformed"]},
          "passed": {"type": "boolean"},
          "decision": {"type": "object"},
          "error": {"type": "string", "maxLength": 65536}
        }
      }
    }
  }
}
