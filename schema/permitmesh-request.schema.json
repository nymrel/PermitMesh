{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/nymrel/PermitMesh/v0.2.0/schema/permitmesh-request.schema.json",
  "title": "PermitMesh Action Request 0.2",
  "type": "object",
  "maxProperties": 256,
  "additionalProperties": false,
  "required": [
    "subject_id",
    "action",
    "repository",
    "ref",
    "files_changed",
    "commands_used",
    "cost_usd",
    "claim_id",
    "fencing_generation",
    "approvals"
  ],
  "properties": {
    "subject_id": {"type": "string", "minLength": 1, "maxLength": 65536},
    "action": {
      "enum": ["read", "edit", "shell", "test", "commit", "review", "deploy", "publish", "spend"]
    },
    "channel": {"type": "string", "minLength": 1, "maxLength": 65536},
    "repository": {"type": "string", "minLength": 1, "maxLength": 65536},
    "ref": {"type": "string", "minLength": 1, "maxLength": 1024},
    "path": {"type": "string", "minLength": 1, "maxLength": 4096},
    "files_changed": {"type": "integer", "minimum": 0},
    "commands_used": {"type": "integer", "minimum": 0},
    "cost_usd": {"type": "number", "minimum": 0},
    "claim_id": {"type": "string", "minLength": 1, "maxLength": 65536},
    "fencing_generation": {"type": "integer", "minimum": 1},
    "approvals": {
      "type": "array",
      "maxItems": 256,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1, "maxLength": 65536}
    },
    "operation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tool", "arguments"],
      "properties": {
        "tool": {"type": "string", "minLength": 1, "maxLength": 65536},
        "arguments": {"type": "object", "maxProperties": 10000}
      }
    },
    "operation_nonce": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._:-]{16,128}$"
    },
    "at": {"type": "string", "format": "date-time"}
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "action": {
            "enum": ["shell", "test", "commit", "deploy", "publish", "spend"]
          }
        },
        "required": ["action"]
      },
      "then": {
        "required": ["operation", "operation_nonce"]
      }
    }
  ]
}
